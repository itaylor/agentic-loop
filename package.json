{
  "name": "@waterfell/agentic-loop",
  "version": "0.2.0",
  "description": "A lightweight library for running agentic loops with LLMs and tool calling",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "npm run test:integration",
    "test:integration": "node --import tsx --test test/**/*.test.ts",
    "test:watch": "node --import tsx --test --watch test/**/*.test.ts"
  },
  "keywords": [
    "agent",
    "llm",
    "ai",
    "tool-calling",
    "mcp",
    "anthropic",
    "openai",
    "ollama"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^17.2.3",
    "zod": "^4.3.5"
  },
  "peerDependencies": {
    "@ai-sdk/anthropic": "^3.0.36",
    "@ai-sdk/openai": "^3.0.25",
    "ai": "^6.0.0",
    "ai-sdk-ollama": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.21.0",
    "typescript": "^5.7.2"
  }
}
